// Firebase API keys for web apps are intentionally public — they identify
// your project to Firebase's servers. Security is enforced by Firestore
// Security Rules (see firestore.rules), not by keeping this key secret.
firebase.initializeApp({
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
  measurementId: ""
});
